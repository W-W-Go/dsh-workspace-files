# dsh-workspace-files bundle patch layer.
# Applied when this package is listed in a profile's `dsh.profile.bundles`.
# Inserts the workspace-files loader entry (host half + dsh.client browser half).
- insert:
    - id: workspace-files
      name: dsh-workspace-files
